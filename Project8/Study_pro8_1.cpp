#include <iostream>
using namespace std;

int main()
{
	int x = -2; // x 는 변수(variable), 2는 리터럴(데이터 그 자체)

	cout << 1 + 2 << endl; // x + 2 는 피연산자(Opernd)

	int y = (x > 0) ? 1 : 2; // 삼항 연산자

	cout << y << endl;

	cout << "My Home" << endl;

	return 0;
}